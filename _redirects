# Premium URL Management — QORVODE
# Redirects for consistent domain handling

# SPA fallback for client-side routing
/*    /index.html   200

# Ensure trailing slashes (optional — commented by default)
# /about    /about/
# /work    /work/
